[defaults]
nocows = 1
hostfile = hosts
vault_password_file = $HOME/.ansible/private_vault_pw.key

ansible_managed = This file is managed by Ansible.%n
  template: {file}
  date: %Y-%m-%d %H:%M:%S
  user: {uid}
  host: {host}
